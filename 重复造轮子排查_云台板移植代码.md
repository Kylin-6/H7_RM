# 重复造轮子排查：老步兵云台移植代码 vs 框架已有能力

> 范围：合并 RoboMaster_H7（0d6463a）后，`LEGACY_INFANTRY_GIMBAL` 相关新增代码
> （Application/Pitch、Application/Communication、Device/ChassisBoard、Device/DM_IMU、
> Shoot legacy 部分、TransportTask 遥测）逐一对照框架组件。
> 性质：只读分析，未改代码。

## 一、可直接替换的手写实现（行为等价，换掉零风险）

| # | 位置 | 手写了什么 | 框架已有 |
|---|---|---|---|
| 1 | `Pitch.cpp:175-225` `ShapeTrajectoryTarget` | 手写限加加速度 S 曲线（位置/速度/加速度积分 + jerk 限幅 + 到达判定） | `Class_Trajectory`（alg_trajectory）：`Init(Vmax,Amax,Jerk,Dt)` + `Reset(pos)` + `Set_Target_Position`，内部用 double 减少重规划误差，能力完全覆盖且更稳 |
| 2 | `Pitch.cpp:147-169` `ShapeRemoteTarget` | 手写斜率限幅（max_step 逐周期逼近） | `Class_Slope`（alg_slope）：`Set_Target` + 周期 `Get_Out` |
| 3 | `Pitch.cpp:82-93` `ClampTarget` | 手写 min/max 夹取 | `Basic_Math_Constrain`（同文件 373、434 行等处已在用） |
| 4 | `Pitch.cpp:376-389` 两个一阶低通；`Com.cpp:70-87` 两级低通 | 手写 alpha 递推 | `Class_Filter_IIR_First_Order`（alg_filter_iir，给截止频率即可） |

注意：#1/#2/#4 属于原工程控制律的一部分（移植纪律是"系数与计算顺序逐项保持"），
替换前需对照原实现做数值等价验证（host 桩件对比两版输出），确认无误再切。
#3 可以随手换。

## 二、绕过框架机制的（能力缺失，不止是风格问题）

### 5. ChassisBoard 与 DM_IMU 没有接入 Daemon 守护体系（最值得修）
- `chassis_board.cpp:62-66`、`dvc_dm_imu.cpp:112-122/142-143`：各自用
  `HAL_GetTick()` 手写 100 ms 超时判定，设备离线对外部不可见。
- 对照：`dmmotor.cpp:183` `DaemonManager::Register(feedback_daemon)` —— DM 电机
  离线会进入 StatusTask/守护统计；ChassisBoard（板间链路，整机唯一输入源！）和
  DM-IMU（Pitch 唯一反馈源！）离线却无人知晓，只能靠控制层静默停手。
- 建议：两处补 `Daemon::Init + DaemonManager::Register`，`IsOnline()` 改用
  `daemon.IsOnline()`，故障可见性立即对齐其他设备。

### 6. 时间源不统一
- 框架设备层基准是 `SYS_Timestamp`（QD4310 用微秒时间戳），新合并的 sbus/referee
  和云台板移植代码（ChassisBoard/DM_IMU/Pitch/Shoot）用 `HAL_GetTick()`。
  两套时间源混用，跨层比较时会有精度/回绕差异。建议按 fix 清单 #20 统一。

### 7. 数据流通路绕过 Topic（fix 清单 #17）
- `Com.cpp:134-149`：手写 `GimbalCommandChanged/ShootCommandChanged` 逐字段比较 +
  全局 setter `RobotCmd_SetGimbal/SetShoot`。而 `RobotCmd.cpp:106-121` 的 setter
  内部就是"存变量 + 标 Dirty → 周期内 Publish Topic"。Com 完全可以直接持
  Publisher 发 Topic，删掉两层包装与 change-detection（Latest-Value Topic 本身
  就能处理重复值）。

### 8. 遥测不走注册机制（fix 清单 #14）
- `TransportTask.cpp:59-96`：JustFloat 16 通道布局硬编码在任务里；
- `Shoot_GetDebug`（Shoot.cpp:460，13 个出参）与 `Communication_GetRawChannels`
  （Com.cpp:232）是专门的 debug 读出函数；
- `Pitch_GetEncoder`（Pitch.cpp:479-488）用位置逆运算"还原"编码器值，纯粹为喂遥测。
- 三者都是"模块手撕数据给传输任务"，与框架 EricTool/未来遥测注册表方向相悖。

### 9. Shoot 手写状态机 + 手写使能重发
- `Shoot.cpp:101/110`：`FireState/JamState` 裸枚举状态机，未用框架 `Class_FSM`
  （Gimbal 的 `Gimbal_FSM` 已示范用法）；
- `Shoot.cpp:311/332` 附近：使能丢失后 `ENABLE_RETRY_MS` 周期重发使能帧——
  这正是 fix 清单 #3 要上收到 bsp_can 命令生命周期（RELAY 类）的手写补发；
- `Pitch.cpp:99-138` 使能延迟状态机同样裸写 + `HAL_GetTick()`。

## 三、结构性重复（已知，合并后仍未解决）

- **#10 remote_control.c 与 sbus.cpp 双实现并存**（fix 清单 #12）：sbus.cpp 已随
  合并入源列表，remote_control.c 还在编译但云台板不用；
- **#11 板间协议两端各写一份**（fix 清单 #13）：云台板侧 `chassis_board.*` 解码
  0x065，底盘板分支 `gimbal_board.*` 编码，无共享头；
- **#12 Gimbal.h / Com.cpp 内部 `#if LEGACY_INFANTRY_GIMBAL` 板型分叉**（fix 清单 #18）。

## 四、值得肯定的（不是造轮子）

- `Class_ChassisBoard` / `DM_IMU` 的 CAN 接入走了 `BSP_CAN_RegisterCallback`
  注册制——框架正确方向，零框架改动接入；
- Pitch 控制律用了框架 `Class_PID` 和 `Basic_Math_Constrain`；
- Shoot 的电机交互用 `Class_DMMotor` 全套接口（含合并进来的 IsEnabled 语义）；
- TransportTask 遥测发送周期用 `osDelayUntil` 相位对齐，节奏处理正确。

## 五、建议动手顺序

1. **补 Daemon 注册**（#5，板间链路与 DM-IMU 是两条安全攸关链路，故障可见性收益最大）；
2. **Com 直发 Topic**（#7，删全局 setter 包装，为数据流收敛铺路）;
3. **时间源统一**（#6，顺手改，风险低）；
4. **控制律组件对照替换**（#1/#2/#4，先写 host 对比测试验证数值等价，再切换）；
5. 其余归入 fix 清单 #3/#12/#13/#14/#18 的既有批次。
