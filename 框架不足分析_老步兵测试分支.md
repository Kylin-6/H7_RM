# 框架不足分析：老步兵测试分支（底盘板视角）

> 对比对象：`老步兵测试`（0a4a5a1）vs 框架基线 `2c0658c`
> 性质：只读分析，未改动任何代码。
> 分支定位：`老步兵测试` 与 `RoboMaster_H7` 从同一框架基线 `2c0658c` 分出，移植的是老步兵**底盘板**（DM 底盘电机 + SBUS 遥控 + 向云台板下发 0x065/0x070/0x075 板间帧）。它移植在先，暴露的问题比云台板那次更接近框架的"承重墙"。
> 姊妹篇：《框架不足分析_老步兵云台_vs_RoboMaster_H7.md》（云台板视角）。两份报告互为印证，本文只展开新发现。

## 一、这个分支暴露的框架级问题（按严重程度）

### 1. CAN 总线负载无规划、无可观测性（P0）
诊断过程：1 kHz 全速下发时，四路 DM 速度帧 + 云台 MIT 帧 + 三路板间帧共 **8 帧/ms**，超过 1 Mbps 总线约 **7.5 帧/ms** 的承载上限。FDCAN 发送 FIFO 周期性填满，`AddMessageToTxFifoQ` 反复失败，BSP 虽保留待发状态重试，但持续过载下**排在后面的帧长期饿死**——表现为"固定几个电机不响应"。

最终修法是应用层各自把周期降回 2 ms（Chassis / Gimbal / 板间帧三处手改）。框架层面缺失的是：
- **总线带宽预算机制**：注册周期发送时应能估算总线利用率，超限时构建期或运行期告警；
- **Tx FIFO 溢出可观测性**：`AddMessageToTxFifoQ` 失败次数没有进 Daemon/状态上报，过载是靠电机"不转"被人肉发现的；
- **发送 QoS**：安全关键帧（使能、板间状态）与高频数据帧同队列无优先级区分。

### 2. 命令生命周期模型缺失 + CAN 可靠性配置割裂（P0）
诊断过程：使能命令只在状态跳变时入队**一次**，而上电瞬间总线上各电机都在上报状态帧、仲裁激烈，同时框架的 CubeMX 配置 `AutoRetransmission = DISABLE`——使能帧一旦丢失就永远丢失，表现为"电机通信正常但始终未使能"。

两个框架缺口：
- **命令没有"生命周期"分类**：状态跳变型（使能）与周期型（速度）在框架里是同一条 `CAN_Tx_Submit` 路径。应允许命令声明"必须周期重发"或"必须可靠送达"，由框架而非每个应用手写 100 ms 重发补丁（本次是 Chassis、Gimbal 各补了一份）；
- **CAN 可靠性参数在 CubeMX 层**：开启 AutoRetransmission 要改 `Core/Src/fdcan.c`（生成代码）+ `.ioc`，框架 BSP 感知不到。总线级策略（重传、仲裁丢失重试）应该上收到框架 CAN BSP 的初始化参数里。

### 3. 设备"在线"与"可用"语义不分（P0，与云台板分支互证）
第一版补使能逻辑用 `IsOnline()` 判断，结果失效——DM 电机**未使能时也周期上报状态帧**，`IsOnline()` 返回 true，"在线但未使能"的电机被跳过。最终改为无条件周期重发。
- 云台板分支为此给 `Class_DMMotor` 补了 `IsEnabled()`；
- 本分支的修法绕开了它。**两个分支各自补各自的做法，还没合流**——正是分支并行开发的代价，框架应尽早把使能状态机（enable/disable 状态、使能丢失判定、自动重使能策略）收进 Motor 设备层。

### 4. 反馈有效性守卫没有成为框架约定（P1）
诊断过程：云台反馈不可用时角度按 0 处理，坐标旋转算出 -180° 偏差，**底盘速度方向整个翻转**。消息中心 Topic 明明有 `valid` 标志和 `sequence`，但没有任何机制强迫消费端检查。框架级缺一个使用约定或便利设施：读取反馈失败时进入显式的安全路径（保持/归零/锁定），而不是静默用零值参与运算。

### 5. BSP API 行为语义没有文档化（P1）
诊断过程：武装指示灯不亮。老工程 `BSP_WS2812_Set_RGB` 是**设置即发送**，框架 `Set_RGB` 只改缓存、刷新依赖 TIM 10 ms 分发——语义不同但 API 长得一样。最终在 Communication 里补了"设置后立即刷新一次"。
- BSP 接口缺"何时真正生效"的语义文档（同步/异步、最坏延迟）；
- `Set_RGB` 缺显式的立即刷新入口，时效性需求只能靠应用层绕。

### 6. DM 电机驱动对 ID 语义理解不全（P1）
老步兵底盘电机用 `can_id 0x50~0x53`，但框架驱动：
- `Init()` 校验拒绝 `can_id > 0x0F`；
- 反馈匹配按 `data[0] == motor->can_id` **全字节比较**，而达妙反馈帧首字节只携带 ID 低 4 位——高 ID 电机的反馈帧永远匹配不上。

被迫修驱动（`can_id & 0x0F` 匹配 + 放宽到 0xFF）。项目有 `Tests/` 和 host 桩件测试能力，但**驱动解码/匹配逻辑没有进 host 测试用例**——这种纯逻辑 bug 恰好是桩件测试最容易抓住的类别（帧匹配、解码范围、ID 掩码）。

### 7. 板型配置体系开始分叉（P1，两分支叠加效应）
- 本分支：CMakePresets 加 `LegacyInfantry` preset，开关宏 `H7_LEGACY_INFANTRY`；
- 云台板分支：开关宏 `H7_LEGACY_INFANTRY_GIMBAL` + `H7_LEGACY_INFANTRY_GIMBAL_YAW`，互斥靠 CMake `FATAL_ERROR`。

同一台机器人、两个分支、**两套平行发明的板型配置体系**。合并时必然冲突。急缺一份板型配置规范：统一命名、每板一份配置文件（preset + 板级描述），而不是每次移植在根 CMakeLists 和 CMakePresets 里手搓一套。

### 8. 遥控输入双实现并存（P2）
`Remote/` 目录下框架原有的 `remote_control.c` 在本分支已是**死代码**（无任何引用），实际遥控是新拷入的 `sbus.cpp`（291 行）。同目录两套遥控并存、无弃用标记。与云台板分支的发现（SBUS 与板间帧两种输入源无统一抽象）合并成同一个问题：**输入源应该有统一接口**，SBUS/板间转发/视觉只是不同的 Provider。

### 9. 通道映射与极性焊死在设备头文件（P2）
`gimbal_board.h` 里：SBUS 通道索引（火控=5、发射速度=8、Pitch=2）、`GIMBAL_BOARD_FIRE_SWITCH_INVERT` 极性宏、Yaw 编码偏移/倍率全部是编译期宏。通道映射是**整机配置**而非驱动属性，换遥控器或改通道就要改驱动头文件。应下沉到板级/整机配置层。

### 10. 遥测/调试输出无 QoS（P2）
诊断过程：串口调试输出会**打断板间帧节拍**，最终只能整体关闭。调试输出与控制相关通信之间没有优先级、限流或带宽仲裁，"出问题想看日志"和"日志影响控制"二选一。

### 11. 算法库整拷入库，与框架算法存在潜在重叠（P2）
`SpeedPlanning.c/h`（232 行）从老工程整拷进 `Middleware/Algorithm/`，而框架已有 `alg_slope`、`alg_trajectory` 等同类能力。移植者没有复用框架算法库——要么是能力不对等，要么是**算法库的推广和文档不足以让移植者主动选择它**。算法层需要一次收敛：明确每个模块的适用场景，或提供老工程算法的等价替换表。

## 二、两分支合并视角：反复出现的模式

| 模式 | 云台板分支证据 | 底盘板分支证据 |
|---|---|---|
| 调度器缺"输入适配"标准插槽 | `#if` 条件插入 Communication_Init/Update | 无条件插入（空骨架模式），两分支两种做法 |
| 在线≠可用 | 补 `IsEnabled()` | IsOnline 误判导致补使能失效，绕开修 |
| 板间协议无共享头 | ChassisBoard 只收 0x065 | GimbalBoard 发 0x065/0x070/0x075，两端各写各的 |
| 输入源无抽象 | 板间通道当遥控用 | SBUS 与旧 remote_control 并存 |
| 框架文件被迫改 | 7 处 | Control_Task / fdcan.c / .ioc / CMakePresets / CMakeLists |
| 问题靠实测发现 | BMI088 坏件、W25Q64 死等 | CAN 饱和、使能丢失、WS2812 不亮 |

**结构性结论**：云台板分支暴露的是"静态结构"问题（配置层、初始化、回调），底盘板分支暴露的是"动态行为"问题（总线负载、命令可靠性、状态语义、时序）。两类合起来，框架下一个大版本的主线应该是：**板级配置层 + CAN 总线治理（带宽预算/命令生命周期/QoS）+ 设备状态语义（在线/使能/有效三分离）**。

## 三、值得肯定的方面

- 本分支提交信息质量非常高：每个 fix 都带完整根因分析（帧数计算、仲裁时序、语义辨析），可直接沉淀为框架设计依据；
- Com 模块职责收敛做得对：明确"只调 RobotCmd_SetXxx、不直接发布 Topic、不碰电机"，健康互锁（200 ms）显式化；
- 底盘板移植全程**零 `#if` 侵入 Application**（相比云台板分支大量条件编译，本分支用"空骨架默认实现"模式更干净）；
- 板间帧利用框架 CAN 周期通道"同 (总线,ID) 保留最新值"语义，与 BSP 设计契合。
