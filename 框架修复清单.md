# 框架修复清单：两份分支对比分析总结

> 汇总来源：
> - 《框架不足分析_老步兵云台_vs_RoboMaster_H7.md》（云台板视角，静态结构问题）
> - 《框架不足分析_老步兵测试分支.md》（底盘板视角，动态行为问题）
> 两条移植线均已完成使命，下列条目按「优先级 × 工作量」排序，✅ 前置依赖用 → 表示。

## 一、总结

**云台板分支（静态结构）**：移植被迫修改 7 处框架/系统文件（freertos.c、Init.cpp、Pulse 表、callback.cpp、Control_Task、TransportTask、根 CMakeLists）——框架对"板型"零抽象；初始化无失败隔离（W25Q64 死等）；EXTI 回调依赖全局单例；数据流四套并存；遥控输入源与 IMU 无抽象；遥测硬编码。

**底盘板分支（动态行为）**：CAN 总线无带宽预算、FIFO 溢出不可观测（8 帧/ms 超 7.5 上限、帧饿死）；一次性使能命令 + AutoRetransmission=DISABLE 组合丢帧；`IsOnline()` 语义误导（未使能也上报）；`Topic.valid` 无人强制检查（-180° 翻转事故）；BSP API 时效语义无文档（WS2812）；DMMotor 高 ID 反馈匹配 bug；板型配置两分支两套命名；遥控双实现并存；通道映射/极性焊死在驱动头文件；调试输出无 QoS。

**一句话结论：框架的"正确方向"已经存在（CAN 回调注册制、消息中心 Topic、Daemon、分层目录），下一步是把它们从"局部正确"推广成"全局约定 + 配置驱动"。**

## 二、修复清单

### P0 — 直接导致过实机故障，优先修

2026-09-22 云台板补充（#3/#4，待上板复验）：用户确认关闭 FDCAN1 自动重传后，右摩擦轮持续不转；遥测显示两轮在线且已使能，左轮约 -25 rad/s、右轮约 0 rad/s，目标为 -25/+25 rad/s。先恢复 FDCAN1 `AutoRetransmission=ENABLE`（同步 `.ioc`）。现有 Daemon/应用层补发只覆盖掉线或未使能；BSP 在 HAL 接收帧时就确认发送版本，没有跟踪总线发送完成，不能保证仲裁失败后的交付。关闭硬件重传也会取消仲裁失败的帧，参见 [Bosch M_CAN 手册 §3.1.7](https://www.bosch-semiconductors.com/media/ip_modules/pdf_2/m_can/mcan_users_manual_v331.pdf)。因此软件兜底尚不能视为完整替代硬件重传，#3/#4 仍未完成。烧录后持续请求摩擦轮转动，核对右轮速度恢复至 +25±1 rad/s、摩擦轮就绪通道变为 1；具体丢帧过程仍需总线抓包确认。

| # | 修复项 | 具体动作 | 涉及位置 |
|---|---|---|---|
| 1 | CAN 总线带宽预算 | 周期发送注册时按 (波特率, 帧长, 周期) 估算利用率；配置阶段超 70% 告警/报错；提供每总线利用率统计 | `bsp_can.cpp/h`、各设备注册点 |
| 2 | CAN Tx 溢出可观测 | `AddMessageToTxFifoQ` 失败计数入 Daemon/StatusTask，超阈值上报告警 | `bsp_can.cpp`、`StatusTask.cpp` |
| 3 | 命令生命周期模型 | `CAN_Tx_Submit` 增加发送类别：`ONESHOT`（跳变）/ `PERIODIC`（周期）/ `RELAY`（必须周期重发的关键命令）；使能类命令标记 RELAY，由框架自动重发，删掉 Chassis/Gimbal 里手写的 100 ms 补发 | `bsp_can`、`dmmotor`、Chassis/Gimbal |
| 4 | AutoRetransmission 上收框架 | CAN BSP Init 增加重传策略参数，CubeMX 只留最小配置；文档写明"使能丢失"与重传的关系 | `bsp_can.cpp`、`fdcan.c`、`.ioc` |
| 5 | 设备状态三分离 | 统一约定 `IsOnline()`（有反馈）/ `IsEnabled()`（已使能）/ 数据有效性；DM 电机补使能状态机（含使能丢失判定），两分支的两种修法合流进设备层 | `dmmotor`、其他 `Class_*` 设备 |
| 6 | 反馈有效性守卫约定 | 消息中心提供强制模式：`ReadOr(x, fallback_action)` 或约定宏；所有坐标旋转/控制律消费反馈前必须检查 `valid` 与新鲜度；InsTask 等示范落地 | `message_center`、消费端 |
| 7 | Init 失败隔离 | 所有外设 `Init()` 带超时与返回值；失败设备标记 degraded 继续启动；先修 W25Q64 JEDEC 死等 | `bsp_w25q64jv.cpp`、各 BSP Init |

### P1 — 消除结构性重复劳动，下次移植前完成

| # | 修复项 | 具体动作 | 涉及位置 |
|---|---|---|---|
| 8 | 板级配置层（最大项） | 每板一份配置（board legacy_infantry / legacy_gimbal / robomaster…）：声明启用的任务、绑定的外设回调、存在的设备清单；System_Init、freertos 任务创建、Pulse 表、CMakeLists 全部由配置驱动；统一板型开关命名（合并 `H7_LEGACY_INFANTRY` 与 `H7_LEGACY_INFANTRY_GIMBAL` 两套） | 根 CMakeLists、CMakePresets、Init.cpp、freertos.c |
| 9 | 回调注册制统一 | EXTI/SPI/TIM 回调对齐 CAN 的 `(对象, 事件) → callback+context` 注册模式；空槽默认忽略，消除 callback.cpp 里的全局单例守卫 | `callback.cpp`、`bsp_spi`、BMI088 注册文件 |
| 10 | Pulse 表条件注册 | `TIM_1ms_Callback_Table` 改为各模块自行注册条目（编译期或 init 期），调度文件不再感知具体模块 | `TIM_1ms_Task.cpp`、`alg_pulse` |
| 11 | 输入源抽象 | 定义统一 `IOperatorInput`（通道值 + 在线状态 + 失联时间戳）；SBUS / 板间 0x065 / 未来视觉各做一个 Provider；Control_Task 固定"输入适配槽 → RobotCmd → 应用"插槽，不再每板手插 | `Remote/`、`Communication/`、`Control_Task.cpp` |
| 12 | 遥控实现收敛 | `remote_control.c` 标记弃用或删除，`sbus.cpp` 转正为唯一实现（或反之），Remote 目录只留一套 | `Device/Peripheral/Remote/` |
| 13 | 板间协议共享头 | 0x065/0x070/0x075 的 ID、字节布局、编码偏移/倍率、通道索引抽成公共头（如 `Protocol/interboard.h`），云台板接收端（ChassisBoard）与底盘板发送端（GimbalBoard）共用 | 两分支合并时处理 |
| 14 | 遥测注册机制 | 模块把可观测信号注册为遥测通道（名称 → 读取回调）；TransportTask 统一收集组帧；JustFloat 布局不再手写；调试输出加限流/优先级（QoS），不再与板间帧抢节拍 | `TransportTask.cpp`、`Com.cpp`、`sys_debug` |
| 15 | 通道映射/极性出驱动 | SBUS 通道索引、火控极性、Yaw 编码偏移/倍率从 `gimbal_board.h` 移入板级配置 | `gimbal_board.h` → 板级配置 |

### P2 — 规范与卫生，合并窗口处理

| # | 修复项 | 具体动作 |
|---|---|---|
| 16 | IMU 统一抽象 | `IImu`（GetEuler/GetGyro/IsOnline）统一 BMI088(sys_imu) 与 DM_IMU 两套风格；DM_IMU 从 C 自由函数改 Class 或适配层 |
| 17 | 数据流通路收敛 | 输入→命令→反馈全部走 Topic；淘汰全局 setter 直调（`RobotCmd_SetXxx` 保留为 Topic 的薄封装）与专用 debug 读出函数 |
| 18 | Gimbal 板型分叉拆分 | `Gimbal.h` 内 `#if LEGACY_INFANTRY_GIMBAL` 双 struct 拆为两个模块文件，由构建配置二选一 |
| 19 | DMMotor 高 ID 用例补测 | host 桩件测试补：can_id 0x50~0x53 反馈匹配、参数应答识别、解码范围断言（现有 Tests/ 框架直接可用） |
| 20 | BSP API 语义文档 | 每个带时效行为的接口（WS2812、UART、USB）注明"设置→生效"延迟；WS2812 补 `FlushNow()` 立即刷新入口 |
| 21 | 算法库收敛 | SpeedPlanning 与 alg_slope/alg_trajectory 做功能对照，或合并或写明适用场景与替换表 |
| 22 | 任务层解耦设备层 | `user_task.h` 去掉 bsp_* 直接 include，任务文件只依赖调度与消息中心 |
| 23 | `IsEnabled()` 合流 | 云台板分支已加的 `Class_DMMotor::IsEnabled()` 随 #5 一并合入主线 |
| 24 | 构建环境健壮性 | 保留 `-pipe` 修复；CI 中对 tmpfs 空间、工具链版本做前置检查，避免"静默构建失败" |

## 三、建议合并顺序

```
第一批（直接修 bug 类）：#5 IsEnabled 合流 → #7 Init 超时 → #19 桩测试
第二批（CAN 治理）：#2 溢出观测 → #1 带宽预算 → #3 命令生命周期 → #4 重传上收
第三批（结构收敛，配合两分支合并）：#8 板级配置层 → #9/#10 回调与 Pulse 注册制 → #11/#12/#13 输入与协议收敛
第四批（体验与规范）：#14 遥测 → #15 通道映射 → #16~#24
```

> 关键原则：**两个分支合并前先定板型配置规范（#8 的命名部分）**，否则两套 `H7_LEGACY_*` 宏合并时必然二次返工。

---

# 附：RoboMaster_H7 基线版修复清单（合并自 RoboMaster_H7 分支）


> 本清单以 `RoboMaster_H7` 为基线。条目按已实施、近期和长期划分；不包含
> FDCAN AutoRetransmission 配置或迁移工作。

## 已实施：首批可靠性修复

- [x] **初始化失败隔离**：系统公开 `READY / DEGRADED / FATAL` 状态和失败位图；
  TIM4/TIM5 启动失败阻止控制任务运行，BMI088、W25Q64、ADC 失败采用降级启动。
- [x] **BMI088 有界初始化**：Accel/Gyro 芯片 ID 与寄存器回读均为有限重试；
  任一子设备失败时不启动 FIFO、不发布 INS，也不进入姿态闭环。
- [x] **W25Q64 有界初始化**：JEDEC ID 最多重试 5 次（总计约 500 ms）；失败后
  禁止内存映射和后续读写，不影响其他模块启动。
- [x] **INS 新鲜度保护**：Topic 支持按时间戳读取新鲜数据；云台采用 10 ms 阈值，
  失联后 Yaw 输出零电流、Pitch 保持最后内部位置，恢复后自动继续。
- [x] **DMMotor 高 ID 修复**：允许 `0x00~0xFF`，反馈按低四位匹配，并覆盖
  `0x50~0x53`、错误反馈和参数应答测试。
- [x] **DMMotor 状态语义**：`IsOnline()` 仅表示近期收到合法反馈，
  `IsEnabled()` 单独反映协议使能状态，禁止用在线状态代替可用状态。
- [x] **设备状态语义统一**：DJI/QDrive/DMMotor、DJI DBUS、S.BUS 和裁判系统均
  提供 `Online / Enabled / DataValid / Healthy` 查询；被动接收设备的 Enabled
  明确定义为驱动已初始化，不伪造协议中不存在的硬件使能状态。
- [x] **CAN 发送失败统计**：提供提交队列满、周期槽满、硬件 FIFO 满和 HAL
  发送失败的饱和计数及只读快照；不改变现有发送调度策略。

## 近期工作

- [ ] 统一板型宏命名，把板型差异集中到板级配置入口。
- [ ] 共享板间协议定义，统一 ID、布局、缩放和通道映射。
- [ ] 收敛 Remote 目录的重复实现，并把通道映射和极性移出驱动。
- [ ] 将 CAN 统计、启动失败位图接入独立诊断/遥测模块；不扩张 `StatusTask` 职责。

## 长期演进

- [ ] 依据实测负载建立每条 CAN 总线的静态带宽预算，再评估调度和 QoS。
- [ ] 建立板级 Profile，逐步收敛 Init、任务创建和周期回调中的板型条件编译。
- [ ] 为 EXTI/SPI/TIM 建立一致的回调注册及空槽忽略机制。
- [ ] 抽象操作者输入源，统一 SBUS、板间转发和未来视觉输入。
- [ ] 建立遥测注册机制，避免在 TransportTask 中硬编码通道布局。
- [ ] 评估 IMU 接口和算法库重叠后再做收敛，不在可靠性修复中顺带重构。

## 验收基线

- Boundary、Topic freshness、CAN statistics host tests 全部通过。
- `RoboMaster_H7` Debug 固件完整构建通过。
- 初始化阶段不存在等待硬件响应的无限循环。
- `Core/Src/fdcan.c` 与 `H7_BSP.ioc` 不因本清单发生修改。
