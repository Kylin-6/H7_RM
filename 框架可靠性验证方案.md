# 框架可靠性验证方案（基于 RoboMaster_H7 分支现状）

> 对象：`RoboMaster_H7` 分支的 H7_BSP 框架
> 原则：**验证的是真实代码路径**——驱动/算法/调度源码一行不改，只把硬件边界换成桩件；主机侧能确认的与必须上电确认的严格分开。

## 一、现状盘点：已经有什么

### Host 测试雏形（Tests/，自研 CHECK 宏 + HAL 桩，g++ 直接编译）
| 测试 | 覆盖内容 |
|---|---|
| `CAN/can_tests.c` | Tx 提交路径、`submit_queue_full_count` 统计、FIFO 空闲模拟 |
| `Topic/topic_tests.cpp` | Publish/Read 一致性、`ReadFresh` 时序边界（注入可控时钟） |
| `SBUS/sbus_tests.cpp` | SBUS 帧解码 |
| `Boundary/uart_tx_tests.cpp` | UART 发送边界 + 桩件目录复用 |
| `FilterPolynomial / Fuzzy / Trajectory` | 算法正确性（含 Python 生成基准系数） |

### 运行期机制
- `Daemon`/`DaemonManager`（32 槽静态注册表）：设备在线监视、跃迁上报、离线时长统计；`StatusTask` 1 Hz 统一 `CheckAll()`——但**只调度，不执行安全策略**；
- 分层架构（BSP / Algorithm / Device / Application / Task）天然可桩化。

### 缺口（为什么现有资产还不够"验证可靠性"）
1. **电机驱动协议层零回放测试**——DMMotor `can_id 0x50~0x53` 反馈匹配 bug（老步兵测试分支暴露）正是回放测试最容易抓住的类别，现有 CAN 测试只测了 BSP 提交路径，没测任何驱动编解码；
2. Daemon 自身无测试（超时边界、跃迁语义）；EventQueue 满溢行为无测试；
3. **无独立硬件看门狗**（IWDG 未启用），用户代码无 `configASSERT`——程序跑飞无兜底；
4. Daemon 离线后无安全联动验证（离线→电机停/云台 DISABLE 的链路从未被验证过）;
5. 测试无 CI 入口，靠手动编译，容易腐化。

## 二、五层验证金字塔

### L1 Host 单元测试（桩件，秒级，每次提交跑）
扩展 `Tests/`，按既有 CHECK 宏风格补齐。断言清单每类至少覆盖：

**电机驱动回放（最高优先级，防 DMMotor 类 bug 复发）**
- 用桩件记录 `CAN_Tx_Submit` 帧：使能帧 ID/DLC/内容（DM：`FF*7 + FC`）、MIT 帧、速度帧的字节序（IEEE754 小端）与正负方向；
- 用**独立实现的厂商编码公式**合成反馈帧回灌（不许复用被测驱动的转换函数，否则错误自我印证），断言解码往返误差、12/16 位边界（raw=0 → −MAX，raw=满量程 → +MAX）；
- **反馈匹配掩码**：`can_id 0x0F~0xFF` 逐个验证 `data[0] & 0x0F` 匹配与 master_id 区分——直接回归老步兵那个 bug；
- 错误帧隔离：错 ID、错总线、DLC<8 全部丢弃；回调注册键恰为 `(总线, 反馈ID)`；
- 多实例：同总线下不同 ID/feedback 的多个电机互不串扰。

**框架机制**
- Daemon：`timeout` 与 `timeout+1` 两侧边界、离线→在线跃迁、`OfflineDurationMs`；用可控时钟注入；
- EventQueue：压满 8 深度后 `Push` 返回 false、消费后恢复；
- CAN 回调注册表：重复注册、ID 冲突、跨总线同 ID；
- PID/DOB/FSM：目标漂移、死区边界、状态机非法跃迁拒绝。

**跑法**：沿用 `g++ -std=c++17 -Wall -Wextra -I stubs -I <被测目录>` 模式；桩件目录放 `-I` 最前防 include 遮蔽；每个测试目录一个 CMakeLists，根级加 `ctest`/脚本一键跑全部。

### L2 Host 场景回放与故障注入（分钟级，合并前跑）
- **帧序列回放**：按真实时序回放一段实车录制的 CAN/UART 数据（上电仲裁风暴、乱序反馈、CRC 错帧），断言整个 Device→Application 链路的状态机输出；
- **使能丢失注入**：模拟使能帧被吞（对应修复清单 #3），验证 RELAY 周期重发逻辑确实补上了——这是"命令生命周期"修复的验收测试；
- **总线负载仿真**：把所有周期发送按 (周期, 帧长) 汇总算利用率，断言 <70%——修复清单 #1 带宽预算的 host 侧验收；
- **Topic 压力**：生产者/消费者两线程 + 模拟中断时序，验证 PRIMASK 临界区语义（host 上可用 std::thread 近似）。

### L3 目标板在机测试（调试器在场，小时级）
- **FDCAN 回环自测**：上电自检用 loopback 模式自发自收，替代"等第一帧真机反馈才知道总线通不通"；
- **资源水位**：FreeRTOS `uxTaskGetStackHighWaterMark` 全任务上报 + CPU 占用率（runtime stats）进遥测，长跑观察是否有增长趋势（内存泄漏/优先级反转的早期信号）；
- **故障落点可观测**：HardFault/NMI/BusFault handler 里记录 LR/PC/栈顶进遥测再复位，别让死机只表现为"没反应"；
- UART/USB/CAN 波特率与采样点实机核对（时钟 ÷ (Prescaler×(1+TSEG1+TSEG2))），对照厂商上位机的收发计数。

### L4 运行期自检与自愈（框架机制，长期在机生效）
- **启用 IWDG + 喂狗链**：最低优先级任务喂狗（任一高优任务卡死 → 复位而非冻结）；复位原因（RCC->RSR）上电读出进遥测，区分上电/看门狗复位/软复位；
- **Daemon→安全策略联动验证**：实机拔掉电机反馈线，确认 N ms 内云台 DISABLE/电机停——把"离线检测"升级为"离线处置"并验证时限；
- **上电自检序列**：外设 Init 全部带超时（W25Q64 教训），失败设备 degraded 启动并上报，验证缺件上电不死机。

### L5 整机与长跑（赛场前）
- 拔插类：遥控器失联 200 ms 互锁、板间线拔插、CAN 终端电阻配置；
- 电源类：跌压重启、上电瞬间仲裁风暴下的使能补齐（老步兵使能丢失场景的实机回归）；
- 长跑：≥2 h 老化，遥测全程记录（Daemon 跃迁、CPU/栈水位、CAN 统计计数），赛后判读趋势。

## 三、与修复清单的验收挂钩

| 修复项（清单编号） | 验收测试 |
|---|---|
| #1 带宽预算 | L2 负载仿真 + 实机 Tx 统计计数不再增长 |
| #2 溢出可观测 | L1 断言失败计数；L3 人为灌满 FIFO 看告警上报 |
| #3 命令生命周期 | L2 使能丢失注入 + 实机上电仲裁风暴回归 |
| #5 状态三分离 | L1 驱动回放断言 IsEnabled；L4 拔线联动时限 |
| #7 Init 失败隔离 | L1 超时桩件；L3 拔掉 W25Q64 上电不死机 |
| #14 遥测机制 | L3 水位/CPU/复位原因经新遥测通道可见 |

## 四、host 已确认 vs 必须上电确认（判读边界）

- **host 可确认**：帧格式/ID/DLC/字节序、解码范围与边界、错误隔离、超时边界、队列/表溢出行为、带宽预算计算；
- **必须上电确认**：电机内部参数（以厂商上位机读数为准，别信旧工程常量）、波特率采样点与终端电阻、供电与时序、仲裁下的真实丢帧行为、EMC 表现。

## 五、落地顺序建议

1. 先补 **电机驱动回放测试**（L1，DMMotor bug 回归用例）——资产复用度最高；
2. Tests/ 根级一键脚本 + Git 提交钩子/CI 跑 L1（半小时接入，防测试腐化）；
3. 启用 IWDG + 复位原因遥测（L4，改动小收益大）；
4. Daemon 联动安全策略 + 实机拔线验证（L4）;
5. L2 回放与负载仿真跟随修复清单 #1/#3 一起落地。
